{
  "name": "idevlab-home-page",
  "version": "1.0.0",
  "private": true,
  "description": "idevlab's personal home page — terminal-styled, WebGL, data straight from GitHub",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-github.mjs",
    "build": "node scripts/fetch-github.mjs",
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  },
  "engines": {
    "node": ">=18"
  }
}
